//tesing benchmark
/*buf = buffer_create(40000,buffer_fixed,1);
time = get_timer();
log("---")
repeat (3000)
    {
    buf = time;
    time = buf;
    }
log(get_timer()-time);*/
/*time = get_timer();
repeat (10000)
    {
    }
log(get_timer()-time);*/

//file = 0;

/*file = get_open_filename_ext("","","","he");

if !string_length(file) 
    log("cancel");
else log("loaded");
log(file)*/
