v1.10.3 (Only released on macOS)
- Fix audio on timeline projects not working on some Mac computers.
- Fix error message after closing the program when using IDN on Mac computers.
- Improved laser optimization, especially on text and imported objects with sharp corners.
- Fix IDN DAC scanning not working with obscure IP address netmasks.
- Overlap parts of drawn circles slightly, to prevent small defects when outputting.
- Other minor changes.

v1.10.2
- Add undo/redo-ing for changing scope in editor mode, and start/end in timeline mode.
- Fix small bug in LaserCube communication causing every 256th frame to be non-conformant. 
- Fix dialog popups on macOS not working on older macOS versions.
- Remember default FPS settings between startups.