if (instance_exists(obj_dropdown))
    exit;
if (!visible)
    exit;
	
dropdown_live_slider();
    
//ilda_dialog_num("fps","Enter the playback speed, in number of frames per second:",controller.projectfps);

